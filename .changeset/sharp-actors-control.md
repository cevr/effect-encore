---
"effect-encore": patch
---

Expose actor-bound `Control` services from entity layers so applications can
run mailbox control operations without threading cluster storage and address
resolver requirements through their own services.
