---
"effect-encore": patch
---

Migrate the lint and typecheck toolchain to the canonical Effect stack (oxlint-plugin-effect via jsPlugins, TypeScript 7, `@effect/tsgo` diagnostics) and clear the resulting violations. Behavior is unchanged.
