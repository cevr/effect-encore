---
"effect-encore": minor
---

Upgrade the runtime to Effect 4.0.0-beta.106.

Add the new `EntityNotAssignedToRunner` cluster routing error to durable send operations.

Require the Effect `Crypto` service in SQL message storage layers.
