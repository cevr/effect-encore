{
  "name": "encore-ds",
  "version": "0.0.0",
  "private": true,
  "description": "Declarative actors + durable workflows on DurableTable + Durable Streams (no @effect/cluster).",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "typecheck:vendor": "tsgo --noEmit -p tsconfig.vendor.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@durable-streams/client": "workspace:*",
    "@durable-streams/state": "workspace:*",
    "@tanstack/db": "^0.6.0",
    "effect": "4.0.0-beta.75",
    "eventsource-parser": "^3.0.0"
  },
  "devDependencies": {
    "@durable-streams/server": "workspace:*",
    "@types/node": "^22.10.0",
    "@typescript/native-preview": "7.0.0-dev.20260507.1",
    "typescript": "^6.0.3",
    "vitest": "^3.2.0"
  }
}
